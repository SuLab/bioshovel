articles(
    doc_id              text,
    content             text,
    article_archive     text,
    article_filepath    text,
    corenlp_filepath    text,
    pubtator_filepath   text
).

sentences(
    doc_id         text,
    sentence_index int,
    sentence_text  text,
    tokens         text[],
    lemmas         text[],
    pos_tags       text[],
    ner_tags       text[],
    doc_offsets    int[],
    dep_types      text[],
    dep_tokens     int[]
).

